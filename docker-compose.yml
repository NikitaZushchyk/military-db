services:
  app:
    build:
      context: .
      dockerfile: docker/php/Dockerfile
      args:
        user: laravel
        uid: 1000
    image: military-db-app
    container_name: military_app
    restart: unless-stopped
    working_dir: /var/www
    environment:
      - SERVICE_ROLE=core
    volumes:
      - ./src:/var/www
    networks:
      - military-net

  web:
    image: nginx:alpine
    container_name: military_web
    restart: unless-stopped
    ports:
      - "8080:80"
    volumes:
      - ./src:/var/www
      - ./docker/nginx/conf.d:/etc/nginx/conf.d
    depends_on:
      - app
    networks:
      - military-net

  front:
    image: node:20-alpine
    container_name: military_front
    working_dir: /var/www
    volumes:
      - ./front:/var/www
    ports:
      - "5173:5173"
    command: sh -c "npm install && npm run dev -- --host"
    networks:
      - military-net

  db:
    image: mysql:8.0
    container_name: military_db
    restart: unless-stopped
    environment:
      MYSQL_DATABASE: military_db
      MYSQL_ROOT_PASSWORD: root
      MYSQL_PASSWORD: password
      MYSQL_USER: laravel
    ports:
      - "3306:3306"
    volumes:
      - dbdata:/var/lib/mysql
      - ./docker/mysql/init.sql:/docker-entrypoint-initdb.d/init.sql
    networks:
      - military-net
    command: --default-authentication-plugin=mysql_native_password --log-bin-trust-function-creators=1

  redis:
    image: redis:alpine
    container_name: military_redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    networks:
      - military-net

  elasticsearch:
    image: docker.elastic.co/elasticsearch/elasticsearch:8.11.1
    container_name: military_elastic
    environment:
      - discovery.type=single-node
      - xpack.security.enabled=false
      - "ES_JAVA_OPTS=-Xms512m -Xmx512m"
    ports:
      - "9200:9200"
    volumes:
      - elastic_data:/usr/share/elasticsearch/data
    networks:
      - military-net

  rabbitmq:
    image: rabbitmq:3-management-alpine
    container_name: military_rabbitmq
    ports:
      - "5672:5672"
      - "15672:15672"
    networks:
      - military-net

  service_logger:
    build:
      context: .
      dockerfile: docker/php/Dockerfile
      args:
        user: laravel
        uid: 1000
    image: military-logger-image
    container_name: military_logger_app
    restart: unless-stopped
    working_dir: /var/www
    environment:
      - SERVICE_ROLE=logger
    volumes:
      - ./services/logger:/var/www
    depends_on:
      - rabbitmq
      - db
    networks:
      - military-net

networks:
  military-net:
    driver: bridge

volumes:
  dbdata:
  elastic_data:
    driver: local